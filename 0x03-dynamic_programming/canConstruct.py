'''
write a function `canConstruct()` that takes in a targetString and
an array of strings.
said fn should return a boolean indicating whehter or not `targetString`
van be constructed from the array of strs
'''